org.gradle.jvmargs=-Xmx8G
fabric.loom.multiProjectOptimisation=false
systemProp.file.encoding=utf-8

# When true, `bundle-compat-modules` nests every *-compat.jar inside the
# Core jar via Fabric's jar-in-jar mechanism, so distribution is a single file.
should_bundle_compat_modules=true